{
  "name": "lesson-source-builder",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c teal,blue \"npm run dev --workspace server\" \"npm run dev --workspace frontend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:server": "npm run dev --workspace server",
    "test": "npm run test --workspace frontend && npm run test --workspace server",
    "prototype": "python3 -m http.server 4173 --bind 127.0.0.1",
    "preview:prototype": "python3 -m http.server 4173 --bind 127.0.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
